{
  "name": "neelkosh-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Blue Carbon Registry & MRV smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy-local.ts --network localhost",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "seed:local": "hardhat run scripts/seed-demo-data.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.2",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.19",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  }
}
